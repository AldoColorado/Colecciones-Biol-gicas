import "./register-button.css";

const RegisterButton = ({ text }: { text: string }) => {
  return (
    <>
      <>
        <div className="register-button">{text}</div>
      </>
    </>
  );
};

export default RegisterButton;
